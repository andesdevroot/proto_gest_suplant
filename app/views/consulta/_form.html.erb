
<%= simple_form_for(@consultum) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<div class="form-inputs">
    <%= f.input :tema,  as: :radio_buttons,
    collection: ['laboral', 'personal', 'beneficio sindical'] %> 
    <%= f.input :diregente,  as: :radio_buttons,
    collection: ['Clodomiro Vasquez', 'Orlando Diaz', 'Luis Rebolledo'] %> 
    <%= f.input :descripción %>
    <%= f.input :fecha %>
    <%= f.association :socio %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>


