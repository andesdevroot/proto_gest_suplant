<%= form_with(model: socio, local: true) do |form| %>
  <% if socio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(socio.errors.count, "error") %> prohibited this socio from being saved:</h2>

      <ul>
      <% socio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :NOmbre %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :Sap %>
    <%= form.text_field :sap %>
  </div>

  <div class="actions">
    <%= form.submit " Crear Socio", class: "btn btn-success" %>
  </div>
<% end %>



