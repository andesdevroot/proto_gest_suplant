
<%= simple_form_for(@maquina2) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :area %>
    <%= f.association :socio %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
